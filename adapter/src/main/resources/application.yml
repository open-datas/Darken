server:
  port: 8083
spring:
  application:
    name: adapter
  http:
    encoding:
      charset: UTF-8
      enabled: true
      force: true
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8081/eureka
    healthcheck:
      enabled: true
